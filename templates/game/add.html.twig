{% extends 'base.html.twig' %}

{% block title %}Home{% endblock %}

{% block body %}

<div class="admingameadd">
<h1>Ajout d'un nouveau jeu</h1>
    {{ form_start(formView, {'attr': {'novalidate': 'novalidate'}}) }}
        {{ form_widget(formView) }}
        <button type="submit">Valider</button>
    {{ form_end(formView) }}
</div>

{% endblock %}
